# Changelog

All notable changes to NexusPilot will be documented in this file.

The format is inspired by Keep a Changelog and uses NexusPilot release categories: Feature, Optimization, and Fixed. NexusPilot follows a product-complete, iterative delivery path, so release notes should focus on user-visible behavior, compatibility, migrations, updater impact, packaging changes, and product polish.

## [Unreleased]

### Feature

### Optimization

### Fixed

## [0.10.2] - 2026-09-02

### Feature

- NexusPilot Cloud 新设备授权现会自动轮询审批结果，并在获批后完成设备认领，无需反复手动刷新授权状态。
- 连接配置新增备注，支持编辑最多 50 个字符的说明并通过 NexusPilot Cloud 同步；在 Explorer 中悬停连接时，可直接查看驱动相关信息、标签和备注。
- AI 对话新增附件支持，可上传图片、音频和文档等文件并随消息发送、保留在历史记录中；附件是否可被识别由当前模型及其供应商决定，不支持时会展示其返回的错误信息。
- AI Runtime 与桌面应用版本不匹配时，设置页会显示明确提示，并提供前往官方下载页面的入口。
- Navigation Rail 新增文档入口，“关于”面板新增 NexusPilot 产品网站入口，均通过系统默认浏览器打开。

### Optimization

- 应用启动并恢复登录会话后，会在后台刷新 Cloud 账户、订阅和同步状态，减少缓存信息过期带来的状态偏差。
- 优化版本更新入口：标题栏提示支持在鼠标悬停或键盘聚焦时展开，便于直接查看可用版本和更新操作。
- 优化 AI 对话交互：附件下载会打开系统保存对话框并反馈结果；用户消息悬停时可直接复制；输入框使用更通用的提示文案。
- 恢复 Oracle Database 与 Amazon Neptune 的品牌图标，提升数据库类型辨识度。

### Fixed

- 修复小窗口下设置菜单无法完整滚动，以及操作下拉选择器时可能误关闭设置窗口的问题。
- 修复更新日志区域无法正确自适应高度，以及内容标签栏无法按预期横向滚动的问题。
- 修复历史消息中的图片附件无法显示缩略图、单击后无法打开预览，以及 AI Runtime 持续重复请求同一附件内容的问题。
- 修复标题栏展开式更新提示中文字位置偏移的问题。

## [0.10.1] - 2026-08-18

### Feature


### Optimization


### Fixed

- 修复 MySQL 元数据数值类型解码兼容性，Explorer 元数据浏览与主键读取在不同 MySQL 配置下不再因类型不匹配而失败。
- 修复同步已暂停时无法查看待授权设备和处理新设备请求的问题，暂停状态下同步与安全页面现在可正常加载设备管理内容。

## [0.10.0] - 2026-08-11

### Feature

- NexusPilot Cloud 正式上线，支持资产端到端加密同步。已注册用户、新用户将免费获得 90 天订阅！欢迎提出优化问题！
- 新增 NexusPilot Cloud 端到端加密同步，支持在已授权设备之间同步连接与文件夹数据；Cloud 仅保存密文，无法读取连接内容。
- 设置新增 Cloud 概览与同步与安全面板，可查看订阅与存储用量、管理同步开关、暂停与恢复同步、处理冲突、补充本地文件路径、管理已授权设备，以及使用恢复密钥重建访问。
- 账号卡片新增 Cloud 使用情况摘要，展示当前订阅、有效期、设备数量和加密存储用量，并可直接跳转 Cloud 设置。
- 工作台状态栏新增 Cloud 连接状态与同步状态指示，覆盖已连接、离线、同步中、冲突待解决、配额已满和设备已撤销等场景。
- 新设备加入同步时，支持通过已授权设备输入短验证码完成批准；无可用设备时可使用恢复密钥重建同步访问。

### Optimization

- 登录凭据改用系统 Keychain 存储，替代原有的加密文件存储，提升账号凭据安全性与跨平台一致性。
- 改进 OIDC 错误处理，区分访问令牌无效、权限不足和暂时不可用等失败原因，登录和会话恢复的错误提示更准确。
- 优化账号卡片 Cloud 提示文案，登录后可查看订阅和 Cloud 使用情况，不再显示"即将开放"。

### Fixed

## [0.9.2] - 2026-08-04

### Feature

- SQLite 连接现在支持通过系统文件选择器定位本地数据库文件，同时保留手动输入路径的方式。
- AI 助手新增网络访问范围偏好：默认仅访问公网；按需允许本地网络后，新的对话可读取内网资源并进行主机连通性检测。
- AI 模型列表现可展示输入/输出模态、附件、结构化输出和交错式推理等能力信息，帮助选择更适合当前任务的模型。
- 关闭工作台时，若存在未保存修改、运行中的工作台任务或 AI 回复生成，会先显示统一确认；确认关闭后会安全中断活跃 AI 运行，避免数据或对话状态意外丢失。

### Optimization

- 优化 AI 供应商目录刷新体验：设置页会显示加载状态和上次刷新时间；远端目录暂时不可用时，仍会继续使用已有的可用缓存。
- 优化 macOS 工作台标题栏布局，保留更新入口与可拖拽区域，避免品牌元素与系统窗口控件相互干扰。
- 优化工作台整体滚动边界与表单布局，触控板在非滚动区域操作时不再触发页面回弹。

### Fixed

- 修复自定义 OpenAI 兼容供应商的工具调用支持：默认可使用工具调用，并可在设置中检测接口兼容性或按需关闭。
- 修复自定义供应商模型较多时设置对话框内容溢出的问题，标题和保存操作在小窗口中仍保持可见可用。
- 修复通知可能被设置等模态窗口遮挡的问题，弹窗打开时通知仍可见并可交互。

## [0.9.1] - 2026-08-03

### Feature

- Agent 模式新增受控原始 SQL 执行，支持 SQLite、MySQL、PostgreSQL、Oracle 和 ClickHouse 的单语句操作；执行前会分析风险并展示一次性执行计划，写入、DDL 和风险不确定的语句需先确认。
- 新增高风险工具审批流程：助手会展示操作目标、风险与执行摘要，用户可批准或拒绝，并在同一轮对话中继续执行后续步骤；关键操作要求输入明确确认文本。
- AI 助手新增 Redis 单键变更能力，支持创建、更新、重命名、设置过期时间和删除 Key；修改采用原子校验，避免审批后误覆盖已变化的数据。
- 新增工具自动审批阈值设置，可按偏好决定低风险或中风险工具是否自动执行；高风险与关键操作仍始终要求确认。
- 支持从任意历史用户消息重新发送，可保留原文重新生成或编辑后以该消息为起点继续对话。
- 新对话会自动生成简短标题；模型选择器新增当前会话上下文用量和 Token 明细展示。
- 自定义 AI 供应商现可从兼容接口发现可用模型并回填到设置，同时保留手动添加模型的方式。
- 新增智能体后台系统通知，可在助手完成或失败时推送会话标题与回复摘要，并可分别控制通知开关。

### Optimization

- 智能体运行状态已整合到工作台底部状态栏；工具审批卡片在待处理时自动展开、批准后自动折叠，减少长结果对对话区域的占用。
- 优化账号面板与头像加载体验，统一账户中心入口、改进首字符兜底显示，并兼容 Clash/Mihomo TUN Fake-IP 网络环境。
- 优化工具调用与确认后的恢复体验，工具级错误会保留为可读结果，让助手能够解释问题或调整方案，而不是直接终止整轮对话。

### Fixed

- 修复高风险 SQL 的风险识别与审批边界：PostgreSQL `EXPLAIN ANALYZE`、数据修改型 CTE 等语句现在会进入强确认，避免被错误视为低风险操作。
- 修复 MySQL 查询结果中的 NULL、Decimal、JSON、浮点、日期时间、二进制和 Geometry 等类型解码，DataTable、SQL 编辑器与 AI 工具现在得到一致结果。
- 修复审批续跑时工具调用状态无法正确匹配的问题，并修复工具执行或预执行计划出错时整轮对话被异常终止的问题。
- 修复 Redis TTL 工具的参数兼容性，设置 Key 过期时间可正常提交。
- 修复智能体 Markdown 导出固定写入默认位置的问题，现可通过系统保存对话框选择导出文件；回复中的网页链接也会改由系统默认浏览器打开。

## [0.9.0] - 2026-07-28

### Feature

- AI 助手已实现首个功能闭环：可在安全边界内理解任务、选择合适模式、读取已保存连接和数据库结构、查询表或 Redis 数据，并将执行过程与结果同步呈现到工作台。
- AI Runtime 实现 `Ask`、`Query`、`Agent` 三种模式：Ask 专注问答与资料检索；Query 可安全完成数据库只读协作；Agent 可在当前已提供的受控能力内主动拆解并推进任务。
- AI 助手现可读取已保存的连接信息并按需打开连接，逐层浏览数据库对象，以及查看关系表的列、索引和约束；由 AI 打开的连接会同步显示到工作台。
- AI 助手新增受控数据浏览能力：可按列、筛选、排序和分页读取表、视图与物化视图数据，也可扫描 Redis Key 并读取类型化值；这些读取操作不接受原始 SQL 或 Redis 命令。
- Agent 对话新增可折叠的执行过程，将思考与连续工具调用汇总展示，并通过工具卡片呈现实际执行结果和状态。
- 新增 AI 对话偏好设置，支持控制启动时的对话恢复，并可在普通列表与长对话虚拟列表渲染之间切换。

### Optimization

- 智能体会根据任务引导选择满足需求的最低风险模式，并更清楚地区分模式限制、连接不可用、网络异常和能力缺失等情况。
- 优化本地 AI Runtime 的安全性与稳定性：每次启动均使用临时访问凭证保护本地接口，并自动维护与桌面后端的能力连接，降低工具调用因连接中断而失败的概率。
- 优化数据库连接状态同步：工作台会在启动、回到前台或由 AI 打开连接后自动对账运行时状态；断开连接时同步清理过期的元数据、标签页和缓存。

### Fixed

- 修复 Agent 会话历史恢复与消息发送在后台健康检查或模型列表刷新期间可能被阻塞的问题。
- 修复启用 Clash/Mihomo TUN 双栈 Fake-IP 时，AI 网络检索可能错误拒绝可访问站点的问题。
- 修复从后端运行时快照恢复连接后，Explorer 可能无法继续加载子节点的问题。

## [0.8.1] - 2026-07-20

### Feature

- 新增可选账号登录功能，支持通过系统浏览器完成登录、桌面回调、应用重启后的会话恢复和标题栏账号卡片；未登录或账号服务不可用时，本地工作台仍可正常使用。
- 新增应用内通知设置，支持调整弹出位置、自动关闭时长、最大可见数量、关闭按钮和丰富颜色，并可即时测试效果。
- Agent 模型选择器新增“管理模型”快捷入口，可直接前往供应商与模型设置页维护可用模型。

### Optimization

- 更新“关于”面板的反馈渠道，新增支持邮箱，便于提交产品问题与建议。

### Fixed

- 修复新建连接与连接属性中 ClickHouse 仍显示为“即将支持”的状态，现已按可用驱动展示。
- 修复连接树带有计数徽标等尾部内容时的宽度溢出，以及 ScrollArea 内容宽度异常导致的横向布局问题。

## [0.8.0] - 2026-07-18

### Feature

- 新增 ClickHouse 单节点 HTTP `IP:PORT` 连接支持，提供用户名/密码认证、连接测试、运行时健康状态和连接恢复能力。
- ClickHouse 接入 Explorer 元数据浏览，支持查看数据库、表、视图、物化视图、列、函数、字典、Projection、Data-skipping Index 和分区信息，并安全展示超大整数、Decimal、时间、Enum、Nullable、LowCardinality、Array、Map、Tuple、Nested、JSON 和 Variant 等类型。
- ClickHouse DataTable 支持表、视图和物化视图的数据浏览、分页与统计；普通 Local `MergeTree` / `ReplacingMergeTree` 表支持基础新增、修改和删除，并提供行快照定位、匹配核验、表名删除确认及冲突或未知结果下的草稿保护。
- ClickHouse SQL 编辑器支持查询和显式 DDL/DML、顺序脚本、执行进度、服务端查询取消与执行超时，并支持 CSV、JSON、Parquet 等原始结果的有界预览和安全另存。
- 新增 ClickHouse 原生结构设计器，支持 Database、Table、Column、Projection 和 Data-skipping Index 的结构查看、DDL 预览及受控创建或变更。
- 新增 ClickHouse View 设计能力，支持 Local View、Materialized View 和标签页级 Temporary View 的结构查看、创建、修改、重命名与删除。

### Optimization

### Fixed

## [0.7.0] - 2026-07-10

### Feature

- 新增 SQLite 本地文件连接支持，可配置数据库文件路径和只读/可写模式，并支持连接测试、ping 与版本读取。
- SQLite 接入 Explorer 元数据浏览，支持按文件数据库展开表、视图、列、索引和触发器，并隐藏 SQLite 内部对象。
- SQLite 支持 DataTable 表/视图数据浏览、分页统计和 SQL 编辑器单语句执行。
- SQLite DataTable 支持普通表新增、编辑、删除、DML 预览和保存，写入范围按可写连接、真实表、显式主键和列元数据限制。
- SQLite DataTable 支持事务开始、提交和回滚，事务中的浏览、分页统计和数据变更会固定在同一连接上执行。
- 桌面端新增应用单例启动，重复启动 NexusPilot 时会恢复并聚焦已有主窗口。

### Optimization

- 数据库选择器新增 ClickHouse 规划项，提前展示后续支持方向。

### Fixed

## [0.6.1] - 2026-07-09

### Feature


### Optimization

- Redis key 树改用虚拟化渲染，展开大量 key 或 prefix 时滚动更轻量。
- 事务进行中状态改用信息色提示，更容易和普通状态区分。
- 移除标题栏右侧占位头像和分隔线，减少未启用入口造成的干扰。
- About 面板新增临时反馈渠道展示，便于用户快速找到反馈入口。

### Fixed

- 修复宽字段 DataTable 滚动时渲染过多列导致的卡顿问题，宽表浏览和固定列场景更流畅。

## [0.6.0] - 2026-07-08

### Feature

- SQL 编辑器新增脚本执行体验，支持运行全部、运行已选取 SQL、运行当前语句、顺序执行多条 SQL，并可请求停止后续队列。
- SQL 编辑器结果面板支持多语句结果列表，逐条展示等待、执行中、成功、失败或跳过状态，并可切换查看每条 SQL 的结果或错误。
- SQL 编辑器新增结果分页，查询结果支持上一页、下一页、连续行号和当前行范围提示。
- SQL 自动补全新增列级候选，输入表名或别名后的 `.` 时会按需加载对应表列并显示列类型。
- 连接配置新增自定义标签，支持 0-8 字标签文本和颜色选择，并在 Explorer 连接树中显示标签徽标或色点。
- Redis 数据库节点新增 key 数量展示，并在创建或删除 key 后刷新数据库计数。

### Optimization

- 优化 SQL 编辑器运行入口，主运行按钮增加下拉菜单，并在上下文栏提示当前将执行的 SQL 范围。
- 优化 Explorer 连接树节点布局和状态视觉，连接状态、标签和 Redis 计数信息更易扫描。
- 优化设置页关于面板，将“检查更新”按钮移至当前版本更新日志上方。
- 优化表设计器详情面板头部间距，提升紧凑布局下的可读性。

### Fixed

- 修复 SQL 编辑器列补全首次触发时可能不弹出的问题。
- 修复 About 面板中多余分隔线导致的视觉割裂。

## [0.5.0] - 2026-07-07

### Feature

- 新增 Oracle Database 支持，提供连接配置、连接测试、元数据浏览、SQL 编辑器执行和只读结果查看。
- Oracle DataTable 支持普通表数据浏览与可写变更，支持基于主键的新增、编辑、删除、DML 预览和事务提交/回滚。
- Oracle 接入表设计器，支持普通表结构读取、新建表、DDL 预览、常用 `ALTER TABLE` 保存和保存前漂移检测。
- Explorer 普通表节点新增“删除表”操作，MySQL、PostgreSQL 和 Oracle 支持 `DROP TABLE` SQL 预览、二次确认和删除后元数据刷新。
- 表设计器重构为专业化页签，按列、索引、约束、选项和分区组织表结构编辑，并提供结构化列类型编辑。

### Optimization

- Oracle Explorer 根层级简化为 schema/owner 下直接展示资产分组，系统 schema 由后端隐藏，减少无效层级干扰。
- 优化新建连接的数据库选择器，Oracle Database 作为已支持关系型数据库前置展示，并显示 `NEW` 标记。
- 表设计器 DDL 预览改为内容工具栏入口和抽屉展示，支持复制、导出、校验提示和危险变更提示，减少编辑区占用。
- 表设计器新增草稿校验与驱动差异化配置，提前提示重复列、缺失类型、非法精度、无效约束引用和不适用的驱动选项。
- 优化 Oracle SQL 上下文、自动补全和元数据查询，schema-first 场景下的 SQL 执行、表打开和保存查询挂载更准确。
- 后端连接测试和数据库运行时新增诊断日志，连接、ping 和低层 SQL 失败时更容易定位真实库问题。

### Fixed

- 修复 Oracle 表设计器保存前漂移检测对 PK/Unique 约束默认启用状态的误判，避免未改动结构被错误拦截。
- 修复 Oracle 表浏览在可写元数据不可用时无法回退只读路径的问题。
- 修复 Oracle SQL 补全上下文与 schema-first SQL 上下文不一致的问题。
- 修复 Oracle DataTable DML 在日期/时间列和复杂字典查询场景中的真实库兼容性问题。

## [0.4.2] - 2026-07-05

### Feature

### Optimization

- 精简 NavigationRail，移除占位入口并聚焦连接列表。
- 移除占位命令搜索组件。
- 实现状态栏注册制，消除硬编码状态项组件。

### Fixed

- 移除更新弹窗中重复的版本徽标。

## [0.4.1] - 2026-07-05

### Feature

- SQL 编辑器结果栏新增折叠按钮。

### Optimization

- 重构内容标签页为注册制架构，支持生命周期管理和动态挂载。
- 重构内容工具栏为开放注册机制，替代原有封闭枚举。
- 重构 Explorer 节点视觉为注册制，统一图标和样式管理。
- 重构 Explorer 动作为注册制，支持动态贡献和条件启用。
- 重构保存查询挂载规则为注册制，提升资源树扩展性。

### Fixed

- 修复大整数超出 JavaScript 安全范围时显示异常的问题，改为序列化为字符串。

## [0.4.0] - 2026-07-04

### Feature

- 新增 SQL 查询编辑器，支持从连接、数据库或 Schema 上下文新建查询、执行 SQL 并查看结果面板。
- 新增保存查询能力，支持保存、打开、删除查询，并在资源树中按连接、数据库或 Schema 上下文展示。
- 新增 SQL 自动补全，提供关键字、常用片段、数据库、Schema、表、视图和物化视图候选。
- 新增连接高级选项，支持连接超时、SSL/TLS 配置和 SSH 隧道连接。

### Optimization

- 优化资源树查询入口，数据库和 Schema 节点可直接新建查询，未指定上下文的保存查询单独归类展示。
- 优化资源树对象分组显示，将表、视图、函数、索引、触发器等 asset group 本地化为中文标签。
- 优化发布流程，CHANGELOG 使用 Feature、Optimization、Fixed 分类，发布上传显示进度，发布帮助文案覆盖完整语义化版本参数。

### Fixed

- 修复 SQL 查询编辑器并发执行、会话隔离和保存回写场景中的状态覆盖问题。
- 修复 SQL 标识符扫描对双引号名称的处理。
- 修复保存查询入口在连接能力不足或运行时状态不匹配时仍可操作的问题。
- 修复发布上传流程中对对象存储旧错误对象的自动清理风险，改为保留版本化上传和人工确认路径。

## [0.3.2] - 2026-07-02

### Feature

- Windows 安装器新增语言选择，支持简体中文和英文。

### Optimization

- 优化新版本提示文案，通知只展示简要信息，完整更新日志可在更新弹窗中查看。
- 优化更新弹窗的更新日志区域，长内容阅读和滚动体验更稳定。

### Fixed

- 修复更新日志过长时可能撑开弹窗、导致内容溢出的问题。

## [0.3.1] - 2026-06-29

### Feature

- 新增通用代码编辑器对话框并接入 DataTable 单元格编辑。
- 新增随机欢迎语替代固定 thread 欢迎文案。
- 实现对话管理操作与历史视图增强。

### Optimization

- ContentTabBar 使用 ScrollArea 替代自定义隐藏滚动条容器。
- 优化内置 agent 系统提示词和 prompt assembler 文案。
- 合并官网与文档站更新，接入 Starlight 文档站。
- 添加搜索引擎爬取元数据、明暗主题切换和配置化首页产品信息。

### Fixed

- 修复更新提示区域滚动溢出问题。
