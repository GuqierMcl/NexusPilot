Starlight docs collection notes
================================

This directory is the Starlight `docs` content collection.

The nested `docs/` directory is intentional:

src/content/docs/       Starlight collection root
src/content/docs/docs/  public documentation content, mapped to /docs/

Do not move `src/content/docs/docs/` unless the public documentation URL prefix should also change.
